<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.atelier.mapper.ProductMapper">

    <select id="countProduct" parameterType="String" resultType="int">
        select count(pid) as cnt from t_products WHERE product_type=#{productType};
    </select>

    <select id="selectProductList" parameterType="com.example.atelier.dto.PageVo" resultType="com.example.atelier.domain.Product">
        SELECT
            pid,
            product_content AS productContent,
            product_title AS productTitle,
            product_price AS productPrice,
            product_type AS productType,
            product_url AS productUrl,
        FROM t_products ORDER BY pid DESC limit #{page}  , #{psize} ;

    </select>

    <select id="selectProductTypeList" parameterType="String" resultType="com.example.atelier.domain.Product">
        SELECT
            pid,
            product_content AS productContent,
            product_title AS productTitle,
            product_price AS productPrice,
            product_type AS productType,
            product_url AS productUrl,
        FROM t_products WHERE product_type =#{productType};
    </select>

    <insert id="insertProduct" parameterType="com.example.atelier.domain.Product">
        insert into t_products(product_content,product_title,product_price, product_type)
         values
          (#{productContent},#{productTitle} ,#{productPrice}, #{productType});
    </insert>

    <select id="countOrderList" resultType="int">
        SELECT count(ono) as cnt FROM t_orders WHERE order_state='입금대기';
    </select>
    <select id="selectProductDetail" parameterType="Long" resultType="com.example.atelier.domain.Product">
        SELECT
            pid,
            product_content AS productContent,
            product_title AS productTitle,
            product_price AS productPrice,
            product_type AS productType,
            product_url AS productUrl,
        FROM t_products WHERE pid = #{pid};
    </select>





</mapper>